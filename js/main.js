console.log(screen.width, window.innerWidth);
let smallScreen = document.querySelector(".small-screen");
if (window.innerWidth < 576) {
    // alert("pantalla pequeña");
    // smallScreen.classList.add("small");
}
